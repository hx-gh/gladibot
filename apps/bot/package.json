{
  "name": "@gladibot/bot",
  "version": "0.2.0",
  "description": "Gladiatus automation bot using Playwright for session + AJAX for actions",
  "type": "module",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "tick": "node src/index.js --once",
    "loop": "node src/index.js --loop",
    "ui": "node src/index.js --loop",
    "postinstall": "playwright install msedge || playwright install chromium"
  },
  "engines": {
    "node": ">=18.17"
  },
  "dependencies": {
    "dotenv": "^17.0.0",
    "express": "^5.2.1",
    "playwright": "^1.49.1"
  }
}
